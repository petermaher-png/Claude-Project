# Database — matches docker-compose.yml defaults for local dev.
# Swap for a Supabase (or other hosted Postgres) connection string when deploying.
DATABASE_URL="postgresql://pmg:pmg_dev_password@localhost:5432/pmg_solution_engine"

# Anthropic API — powers the recommendation engine (src/lib/claude.ts)
ANTHROPIC_API_KEY=""

# Simple internal-tool access gate until real multi-user auth is needed (Phase 1: single user)
APP_ACCESS_KEY=""
